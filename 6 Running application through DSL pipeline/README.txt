DSL Pipeline will perform below activity
	Pull the image from docker hub repository
	run the nodejs application in docker container 
	
Once these steps is completed we can access the application in browser with the url instanceip:8081 port.

Because we have exposed the application in 8081 port via dockerfile